# Copy to .env and adjust for local development (never commit .env)

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/inventoryhub
REDIS_URL=redis://localhost:6379

# Used to sign and verify JWTs — must be long and random in production
JWT_SECRET=change-me-in-production-min-32-chars
JWT_EXPIRES_IN=8h
